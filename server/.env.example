# 小树成长 API Server

PORT=8000
DB_PATH=./data/app.db

# PostgreSQL (production)
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=little_tree_growth
# PGUSER=app
# PGPASSWORD=your_password

# JWT
JWT_SECRET=change_this_to_a_random_secret
JWT_EXPIRES_IN=30d
